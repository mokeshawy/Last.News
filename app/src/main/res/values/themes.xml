<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LastNews" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/apricot</item>
        <item name="colorPrimaryDark">@color/apricot</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Customize your theme here. -->
    </style>


    <style name="mySpinnerItemStyle" parent="@android:style/Widget.Holo.DropDownItem.Spinner">
        <item name="android:textSize">13sp</item>
        <item name="android:textColor">@color/silver</item>
        <item name="background">@color/silver</item>
        <item name="android:itemPadding">15dp</item>
    </style>
</resources>